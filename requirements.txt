# Core runtime dependencies
pyyaml>=6.0

# Optional: required only when provider=openai
# openai>=1.30

# Development & testing
pytest>=8.0
pytest-cov>=5.0
black>=24.0
pylint>=3.0
mypy>=1.10
types-PyYAML>=6.0
